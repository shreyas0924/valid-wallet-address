{
  "name": "valid-wallet-address",
  "version": "0.0.9",
  "description": "Is the wallet address valid or not",
  "main" : "dist/index.js",
  "module" : "dist/index.mjs",
  "types" : "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "tsc"
    
  },
  "license": "MIT",
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12"
  }
}
